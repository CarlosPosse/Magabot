local_costmap:
  global_frame: /odom_combined
  
  # MAP MANAGEMENT
  static_map: false
  rolling_window: true
  width: 3.0
  height: 3.0
  resolution: 0.025
  
  # RATE
  update_frequency: 5.0  # Frequency to update the map (Hz)
  publish_frequency: 5.0  # Frequency to display the map (Hz)
  
